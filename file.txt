/*
        for(Feld spawn: SpielfeldHeinz.getSpawnFelder().values()) {
            if(!spawnFelder.containsValue(spawn)) {
                spawnFelder.put(playerId, spawn);
                System.out.println("Spieler " + playerId + " ist im Spawn " + spawn.getId() + " mit runde id " + playerIndex);
                spielerObjekt = new SpielerObjekt(spawn, playerIndex, this);
                spieler.put(playerId, spielerObjekt);
                channel.writeAndFlush("SETSPWN:"+spawn.getId()+":"+ playerIndex +"\n");
                break;
            }
        }*/